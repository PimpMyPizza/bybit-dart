name: bybit
description: A Dart library to communicate with the bybit API
version: 0.2.0
homepage: pub.dev/packages/bybit
repository: https://github.com/PimpMyPizza/bybit-dart
issue_tracker: https://github.com/PimpMyPizza/bybit-dart/issues

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^1.1.0
  crypto: ^2.1.5
  logger: ^0.9.4
  http: ^0.12.2
  sortedmap: ^0.4.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
